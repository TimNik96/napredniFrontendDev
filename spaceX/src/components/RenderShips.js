const RenderShips = (shipList) => {
    shipList.forEach((ship) => {
        // create custom ship element
        // for a role, choose a random arr instance
        // ship_name
        // "roles": [
        //     "Support Ship",
        //     "Barge Tug"
        // ],
        // active - we can use it for filtering active duty ships
        // image
    })
}
